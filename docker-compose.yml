version: '2.3'
services:
  app:
    build: .
    ports:
      - "8984:8984"
    environment:
      - FLASK_ENV=production
    volumes:
      - .:/app
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #       - driver: nvidia
    #         device_ids: ['0']
    #         capabilities: [gpu]
    runtime: nvidia
    command: gunicorn --bind 0.0.0.0:8984 wsgi:app