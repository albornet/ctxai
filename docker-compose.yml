services:
  app:
    build: .
    ports:
      - "8984:8984"
    environment:
      - FLASK_ENV=production
    expose:
      - 8984
    volumes:
      - .:/app
    deploy:
      resources:
        reservations:
          devices:
          - driver: nvidia
            device_ids: ['0']
            capabilities: [gpu]
    ipc: host
    ulimits:
      memlock: -1
      stack: 67108864
    command: gunicorn --bind 0.0.0.0:8984 --threads=8 wsgi:app
